<%= form_with(model: cat) do |form| %>
  <% if cat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cat.errors.count, "error") %> prohibited this cat from being saved:</h2>

      <ul>
        <% cat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :image_url, style: "display: block" %>
    <%= form.text_field :image_url %>
  </div>

  <div>
    <%= form.label :favorited, style: "display: block" %>
    <%= form.check_box :favorited %>
  </div>

  <div>
    <%= form.label :is_adopted, style: "display: block" %>
    <%= form.check_box :is_adopted %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
